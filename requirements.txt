# Data stack
pandas==2.3.3
numpy==2.4.1

# Visualization
matplotlib==3.11.0
seaborn==0.13.2
plotly==6.9.0

# Web interface
streamlit==1.53.1

# Pin native serialization libs to mature versions — the unpinned latest
# builds (protobuf 6.33.x, pyarrow 25) segfault on Streamlit Cloud Linux.
protobuf==5.29.5
pyarrow==21.0.0
pillow==11.3.0

# Configuration (.env loading)
python-dotenv==1.2.2

# LLM SDKs: openai covers OpenAI, Groq and Gemini (OpenAI-compatible
# endpoints); anthropic covers Claude.
openai==2.45.0
anthropic==0.116.0

# PDF report generation
reportlab==5.0.0
